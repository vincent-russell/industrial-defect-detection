# assets/

Curated images committed to the repo (e.g. example "input → predicted mask" figures) for
use in the main README and portfolio.

Unlike `results/` — which is gitignored, bulky, and regenerated — the few images here are
intentionally tracked so they render on GitHub.
