# Python dependencies.
#
# torch / torchvision default to CUDA-enabled wheels. For a specific CUDA
# version, install them first from the official index, e.g.:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Versions will be pinned once the SAM pipeline is validated end-to-end.

torch
torchvision

numpy
pillow
matplotlib
opencv-python
tqdm

# Segment Anything Model (installed straight from GitHub)
git+https://github.com/facebookresearch/segment-anything.git
