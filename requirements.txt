# Python dependencies, pinned to the versions in the local .venv.
#
# torch / torchvision are the default CUDA-enabled wheels. For a different CUDA
# version, install them first from the official index, e.g.:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130

torch==2.12.1
torchvision==0.27.1

numpy==2.5.1
pillow==12.3.0
matplotlib==3.11.0
tqdm==4.68.3
